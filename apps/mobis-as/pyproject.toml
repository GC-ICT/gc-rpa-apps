[project]
name = "mobis-as"
version = "0.1.0"
description = "모비스 A/S 연동 앱"
requires-python = ">=3.12"
dependencies = [
    "selenium>=4.27",
    "gc-rpa-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mobis_as"]
